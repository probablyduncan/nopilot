---

interface Props {
    randomize: boolean;
}

const { randomize } = Astro.props;

const favicons = ["✨", "🤖", "🤮", "😫", "🕳️", "💸"] as const;
const icon = favicons[randomize ?  Math.floor(Math.random() * favicons.length) : 0];
---

<link
    rel="icon"
    href={`
        data:image/svg+xml,
        <svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'>
            <text y='.9em' font-size='90'>${icon}</text>
        </svg>`}
/>
